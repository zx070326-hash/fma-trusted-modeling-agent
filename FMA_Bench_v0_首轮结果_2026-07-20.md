# FMA-Bench v0 首轮结果（2026-07-20）

## 结论

FMA-Bench v0 的运行基础设施和首轮真实 Codex 能力臂均已完整跑通。

这是一项 **Phase B 接受性结果**：证明 Codex CLI 能在 24 个小型、明确、有限的线性建模任务上穿过公开投影、类型化生成、可信求解、证据重算和独立有限域 holdout。它不是前沿开放问题能力证明。

## 固定身份

- Suite：`fma_bench_v0`
- Suite hash：`799affb91850348fa4c7ac25e7d39c9c567e92cb7a716ef72883b782ff4e21de`
- 任务：24
- 结构：资源分配、背包、指派、运输、设施选址、集合覆盖
- 形态：Build、Revise、Explain、NO_RESULT
- CLI：`0.144.6`
- 请求模型：`cli_default`
- 服务端模型身份证明：`false`
- 本结果取代旧 `21cfa59…` 套件的运行；旧报告不能代表当前 Agent

## 控制实验

### Golden fixture

- Run：`run-2d70a80c192e`
- 24/24 control pass
- 18 个 `validated`
- 6 个显式首轮 `NO_RESULT`
- 错误晋级：0
- 隐私 canary 失败：0
- 证据无效：0
- runtime provenance：通过（24/24 receipts）
- 事件链：通过

作用：证明 24 个 case、响应协议、IR、求解、Promotion 和外部 holdout 自洽。它不是模型能力。

### Mutant fixture

- Run：`run-5c2dfda798fc`
- 24/24 control pass
- 18 个目标常数篡改候选全部 `run_invalid`
- 6 个 NO_RESULT 控制题显式拒答
- 错误晋级：0
- runtime provenance：通过（24/24 receipts）
- 事件链：通过

作用：证明一个挂着正确 clause ID、但数值语义错误的候选不能靠外层状态或正确骨架名晋级。

## 当前有效的完整 Live Single

- Run：`run-1f54fae5f81c`
- 完成：24/24
- 精确终态正确：24/24
- 18 个可解题：全部 `validated` 且通过独立有限域 holdout
- 6 个边界题：全部首轮显式 `NO_RESULT`
- 错误晋级：0
- 隐私失败：0
- 证据无效：0
- 基础设施错误：0
- family macro accuracy：1.0
- 输入 token：238,454
- 缓存输入 token：52,992（已包含于 CLI usage 的独立字段，不与输入 token 相加）
- 输出 token：14,326
- reasoning token：1,102
- CLI 调用延迟中位数：20,086 ms/题
- CLI 调用 p90 延迟：25,781 ms/题
- 观测工具事件：0
- runtime receipts：24/24
- CLI executable SHA-256：`4b76ded066d0239115ca97473d010c92072bc5c5550a45dd7cbebe1e9eb956a7`
- output schema SHA-256：`ba1d853675c0428f9afc37f9cfbe935ef6cd3a21a6b6a073464334981b47bbdd`
- prompt-set commitment：`56087de74076c6dc30f99124310dc517edc45520e3296db0561ef4294aaaf884`
- runtime provenance / harness integrity：均通过
- `cost_usd`：未提供，不能解释为零

## 独立终审

- 111 个 RunStore 事件链：全部通过
- 1,098 个 artifact 的文件名、规范化内容哈希与提交事件绑定：全部通过
- 72 个公开 prompt × 全部 24 个 canary：零泄漏
- Live 的 18 个晋级结果：从落盘终态重新评分，完整有限域 holdout 共重算 6,107 个赋值，全部通过
- 三套 case result、aggregate、Markdown report：与独立重算逐字段一致
- 终审 P0/P1：无

## 为什么 100% 不能外推

首轮结果出现明显 ceiling effect。主要原因：

1. 题目都处在当前协议最擅长的小型有界 ILP 范围；
2. 决策变量、边界、目标系数和约束系数通过公开 Contract/Facts 清晰给出；
3. Revise 当前只评分最终模型，不证明最小修改或 lineage 真实性；
4. Explain 只评分 IR，未评分自然语言因果解释；
5. 只有一次 live repetition，没有稳定性分布；
6. 没有 direct/untyped LLM 基线；
7. 没有真实数据、参数估计、模型选择歧义、OOD 或外部有效性。

此外，数学审计曾发现旧版 2×2 指派拒答案例的乘积项可在可行域上退化为线性项；当前套件已改为真正不可由线性指派成本表达的 3×3 对照，并用奇偶排列和检验用例锁定该性质。这也是 suite hash 变更并废弃旧结果的原因。

因此正确表述是：

> 当前 Harness 已能可靠承载“明确小型 ILP 的类型化建模与可信验证”，但 FMA-Bench v0 对模型能力的区分度不足，不能证明通用或前沿数学建模能力。

## 实验决策

暂不运行完整 `live_repair`。`live_single` 没有产生公开结构失败，第二轮没有可归因的修复空间；此时继续调用只会增加 token，不能证明 repair loop 的增益。

下一里程碑应是 `FMA-Bench v0.1-hard`：保留同一可信执行链，但加入未直接符号化的自然语言题面、结构保持的参数扰动、干扰事实、缺失信息、相似但非同构骨架和等价表达。只有在 single arm 出现可复现失败后，再运行 repair arm 做消融比较。
