# I32 blinded-context shadow evaluation

This directory is a same-host `BLINDED_CONTEXT_SHADOW_EVALUATION`, not an FMA V5 gate artifact, scientific result, external private qualification, or promotion decision. `same_host=false` is not established.

The custodian released 28 public daily observations and 4 future target coordinates. Source identity, raw values, target values, transform parameters, private keys, and the decryption secret are excluded from this directory.

Verify every listed public artifact against `hash_manifest.json`, then verify the Ed25519 signature in `custody_attestation.json` using `custody_ed25519_public.pem`. The attestation proves only the stated same-host shadow-custody binding.
